#!/usr/bin/env bash

# Deprecated compatibility shim. The canonical interactive UI lives in
# bc250-master-toolkit; this file is intentionally kept inert for one release
# so older local installations that source it do not introduce duplicate menus.
